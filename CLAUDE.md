# 골프 취소표 알림

개인 규모(운영자 본인 + 소수 지인, 초대코드로만 가입) PWA. 라비에벨 골프장 예약 사이트를
자동으로 감시해 등록해둔 관심 날짜/시간대에 취소표가 새로 열리면 웹 푸시로 알리고, 화면에서
바로 확인해 골프장 사이트로 넘어가 예약을 완료할 수 있게 한다. 자동 예약은 하지 않는다.

## 프로젝트 구조

```
.
├── CLAUDE.md / AGENTS.md          ← 이 파일 (진입점)
├── docs/
│   ├── architecture.md            → 컴포넌트 구성, 대표 요청 흐름
│   ├── business-rules.md          → 도메인 규칙(관심조건, 전환 판정, 알림 규칙)
│   ├── security.md                → 인증/인가/자격증명 관리
│   ├── standards.md                → 지켜야 하는 하드 규칙
│   ├── engineering-notes.md       → 겪은 함정과 비직관적 동작
│   ├── operations.md               → 설정/빌드/배포 절차
│   ├── contracts.md                → API 인터페이스 카탈로그
│   └── tracking/
│       ├── status.md               → 완료/미완료/막힌 부분
│       ├── decisions/index.md      → 트레이드오프가 있던 결정 기록
│       └── findings.md             → 이 세션에서 못 푼 문제
└── src/lib/adapters/AGENTS.md      → 사이트-어댑터 모듈의 경계·불변식
```

## 하드 게이트 (전체 규칙은 docs/standards.md, docs/security.md)

- App User 로그인 비밀번호는 단방향 해시로, FacilityCredential(골프장 계정) 비밀번호는
  가역 암호화로 저장한다 — 절대 서로 바꿔 쓰지 않는다.
- 감시는 User별 개별 계정으로 로그인한다(대표 계정 공용 감시로 "최적화"하지 않는다) — 회원
  등급별로 보이는 잔여 시간표가 다를 수 있기 때문이다. 예외: 어댑터가 `loginless: true`인
  골프장(예: 레이크우드 — 봇 차단 때문에 비로그인 감시). 이땐 계정 없이 비회원 화면으로만
  감시하며, 회원 전용 시간표는 못 볼 수 있음을 사용자가 감수한다.
- 골프장 로그인 실패 시 무한 재시도하지 않는다 — 즉시 알리고 그 계정 감시를 멈춘다.
- 알림은 "신청 불가 → 신청 가능" 전환 시점에만 보낸다 — 이미 열려있는 슬롯을 매 스캔마다
  재알림하면 안 된다.
- 사이트별 로그인/스캔 로직은 `src/lib/adapters/`의 어댑터 안에만 있어야 한다 — 핵심 엔진
  코드에 특정 골프장의 URL/HTML 구조를 직접 쓰지 않는다.

## 작업 전 체크리스트

- 아무 작업이나: `docs/standards.md`, `docs/engineering-notes.md`를 먼저 읽는다.
- 인증/인가/자격증명 저장 관련 작업: `docs/security.md`를 먼저 읽는다 — 두 종류의 비밀번호
  저장 방식(단방향 해시 vs 가역 암호화)을 헷갈리기 쉽다.
- 새 골프장 어댑터 추가나 기존 어댑터 수정: `src/lib/adapters/AGENTS.md`와
  `docs/tracking/findings.md`(실사이트 HTML 미검증 이슈)를 먼저 읽는다.
- 알림/매칭 로직 변경: `docs/business-rules.md`의 "전환 판정" 절을 먼저 읽는다 — 재알림
  방지·재전환 규칙이 비직관적이다.
- DB 스키마 변경: `prisma/schema.prisma`의 `onDelete: Cascade` 관계를 깨뜨리지 않는지
  확인한다(계정 탈퇴 시 연쇄 삭제가 여기 걸려 있다).

## 문제 발생 시

- 다음은 발견 즉시 사용자에게 보고한다: 다른 User의 데이터가 노출/변경되는 경우, 골프장
  계정 비밀번호가 평문으로 저장/로그 출력되는 경우, 계정 탈퇴 후에도 데이터가 남는 경우,
  자동 로그인이 반복 실패해 실제 회원 계정이 잠길 위험이 있는 경우.
- 그 외의 문제(사소한 버그, 개선 아이디어, 확인이 필요한 가정)는
  `docs/tracking/findings.md`에 기록한다.
