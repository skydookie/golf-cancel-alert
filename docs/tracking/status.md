# status.md — 현재 위치

## 완료 (구현 + 테스트 통과)

- App User 인증: 초대코드 가입, 로그인, 로그아웃, 세션 검증, 계정 완전탈퇴 — API + 단위/통합
  테스트(`tests/api/auth.test.ts`, `tests/lib/auth.test.ts`, `tests/lib/crypto.test.ts`,
  `tests/lib/inviteCode.test.ts`).
- 관심조건(Watch Condition) CRUD — API + 테스트(`tests/api/conditions.test.ts`).
- 골프장 계정(Facility Credential) 등록/갱신/삭제, 암호화 저장, 일시중지/재개 — API +
  테스트(`tests/api/credentials.test.ts`).
- 웹푸시 구독 등록/발송 — API + 테스트(`tests/api/push.test.ts`, `tests/lib/push.test.ts`).
- 사이트-어댑터 인터페이스 + 라비에벨 올드코스 구현(로그인, 캘린더 스캔, 날짜별 시간표 스캔,
  딥링크) — 단위 테스트 통과(`tests/adapters/laviebelle.test.ts`)이나, **fixture 자체가
  스크린샷 기반 추정 구조라 실사이트 HTML로는 검증되지 않았다**(아래 "막힌 부분" 참고).
- 감시·매칭·알림 엔진(전환 감지, 재알림 방지, 재전환 재알림, 동시다발 개별 알림, 로그인 실패
  처리) — 8개 시나리오 테스트 통과(`tests/lib/scanCycle.test.ts`).
- 스케쥴 API + 화면(매칭된 슬롯 목록, 최근 알림 이력, 딥링크) —
  API 테스트 통과(`tests/api/schedule.test.ts`), 화면 자체는 수동 스모크 확인만(아래 참고).
- 로그인/가입/설정(관심조건·계정 관리·탈퇴) 화면 — **빌드/타입체크/수동 스모크 테스트만
  통과, 화면 단위의 자동화 테스트는 없음**(아래 "빌드는 됐지만 검증 안 된 부분" 참고).
- PWA(manifest, 서비스워커, 웹푸시 구독 클라이언트 로직) — 빌드 통과, 아이콘은 자리표시용
  단색 PNG.
- GitHub Actions 스케줄 워크플로 작성 — 실제 배포 환경에서 실행해본 적은 없음(로컬에서
  워크플로 문법만 확인 가능).

## 빌드는 됐지만 검증 안 된 부분 (완료로 세지 않음)

- `/login`, `/signup`, `/schedule`, `/settings` 화면의 폼 제출 흐름은 **로컬 프로덕션
  빌드(`next build && next start`) 위에서 수동 curl 스모크로만 확인**했다(정적 렌더링, 비로그인
  리다이렉트, API 401 응답까지만) — 실제 브라우저로 폼을 채워 제출하는 e2e 테스트는 없다.
- 실제 PostgreSQL에 대한 마이그레이션(`prisma migrate dev`) 자체는 실행해본 적 없다(테스트는
  전부 Prisma client를 mock). 스키마가 실제 PostgreSQL에 문제없이 적용되는지는 배포 시 최초
  확인이 필요하다.
- 실제 VAPID 키/실제 브라우저로 웹푸시 수신까지 가는 end-to-end 확인은 하지 않았다(발송
  함수 자체의 로직만 mock으로 검증).

## 막힌 부분 (docs/tracking/findings.md에 상세)

- 라비에벨 사이트의 실제 HTML 구조(캘린더/시간표 화면, 로그인 폼)가 실사이트로 검증되지 않음.
- 뉴코스(`laviebelle-new`)가 올드코스와 같은 구조를 쓰는지 확인되지 않아 등록하지 않음.
- 취소표 클릭 시 날짜별 화면으로 직접 연결되는 딥링크가 실제로 가능한지 확인되지 않아, 현재는
  캘린더 메인 화면으로만 연결하는 안전한 기본값을 쓰고 있음.

## 남은 범위 (계획됐지만 이번 사이클에 구현 안 됨)

- 다른 골프장(라비에벨 외 사이트) 추가 — 어댑터 인터페이스는 준비됐으나 두 번째 구현체 없음.
- 자동 예약/결제 — 의도적으로 범위 밖(이 앱은 딥링크만 제공, 예약은 항상 사용자가 직접).
- 관심조건의 코스(OUT/IN) 필터, 인원수 조건 — 의도적으로 범위 밖.
- 계속 열려있는 슬롯에 대한 주기적 리마인드 알림 — 의도적으로 범위 밖(신규 전환 시에만 알림).
- App User 자신의 로그인 비밀번호 변경 API — 구현되지 않음(비밀번호를 잊으면 현재는 재설정
  수단이 없다 — 개인 규모라 계정 탈퇴 후 재가입으로 대체 가능하지만, 정식 기능은 아님).
